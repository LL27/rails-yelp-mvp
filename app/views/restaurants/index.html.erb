<div class="container">
  <h1>Restaurants</h1>

  <ul>
  <% @restaurants.each do |rest| %>
    <li>
      <%= link_to rest.name, restaurant_path(rest) %>
      <%= rest.address %>
      <%= rest.category %>
      <%= rest.phone_number %>

    </li>
  <% end %>
  </ul>

</div>
